# === Database ===
POSTGRES_USER=apnigully
POSTGRES_PASSWORD=CHANGE_ME_strong_password_here
POSTGRES_DB=apnigully

# === JWT ===
JWT_SECRET=CHANGE_ME_generate_with_openssl_rand_base64_32
JWT_EXPIRY=7d

# === CORS (your VM's public IP or domain) ===
CORS_ORIGINS=http://YOUR_VM_PUBLIC_IP

# === Frontend URLs (used at build time) ===
# These get baked into the Next.js bundle
NEXT_PUBLIC_API_URL=http://YOUR_VM_PUBLIC_IP/api/v1
NEXT_PUBLIC_WS_URL=http://YOUR_VM_PUBLIC_IP

# === MinIO (S3-compatible storage) ===
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=CHANGE_ME_minio_password
